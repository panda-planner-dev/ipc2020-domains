(define (domain test-domain)
	(:requirements :negative-preconditions :typing :hierarchy)

	(:task task1 :parameters ())

	(:method donothing
		:parameters ()
		:task (task1)
		:subtasks (and
		)
	)
)
